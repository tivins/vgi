
#include "trace.h"

using namespace vgi;

int main()
{
    Tracer tracer;
    // tracer.scene.load("test1.scene");
    // tracer.display.load("dpy.conf");
    // tracer.animation.load("anim.conf");
    // tracer.render();
	return 0;
}


